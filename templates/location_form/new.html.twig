{% extends 'layout_boxed.html.twig' %}

{% block title %}Hello LocationFormController!{% endblock %}

{% block content %}

    {# {{ form_start(form) }}
        <div class="row mb-3">
            {{ form_label(form.countryCode) }}
            <div class="col-sm-10">
                {{ form_widget(form.countryCode) }}
            </div>
        </div>

        <div class="row mb-3">
            {{ form_label(form.name) }}
            <div class="col-sm-10">
                {{ form_widget(form.name) }}
            </div>
        </div>

        <div class="row mb-3">
            {{ form_label(form.latitude) }}
            <div class="col-sm-10">
                {{ form_widget(form.latitude) }}
            </div>
        </div>

        <div class="row mb-3">
            {{ form_label(form.longitude) }}
            <div class="col-sm-10">
                {{ form_widget(form.longitude) }}
            </div>
        </div>
    {{ form_end(form) }} #}

    {{ form(form) }}
    
{% endblock %}
